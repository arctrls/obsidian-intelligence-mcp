spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/obsidian_intelligence
    username: ${DB_USERNAME:obsidian}
    password: ${DB_PASSWORD:obsidian}

obsidian-intelligence:
  vault:
    path: ${VAULT_PATH:/vault}
