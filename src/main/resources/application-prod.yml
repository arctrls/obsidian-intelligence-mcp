spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/obsidian_intelligence?currentSchema=public
    username: obsidian
    password: obsidian

obsidian-intelligence:
  vault:
    path: ${VAULT_PATH:/vault}
